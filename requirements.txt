# ── Core ─────────────────────────────────────────────────────────────────────
anthropic>=0.40.0
python-dotenv>=1.0.0
numpy>=1.26.0
Pillow>=10.0.0

# ── OLED display — Inland 1.3" SH1106 (128×64) over I2C ─────────────────────
luma.oled>=3.13.0
luma.core>=2.4.2

# ── Audio — I2S MEMS microphone (sounddevice wraps PortAudio/ALSA) ───────────
sounddevice>=0.4.6
scipy>=1.11.0

# ── Raspberry Pi Camera / AI Camera (IMX500) ─────────────────────────────────
# Install via apt, NOT pip:
#   sudo apt install -y python3-picamera2 python3-libcamera
#   (picamera2 is not on PyPI in a Pi-compatible form)

# ── Hailo AI HAT+ (Hailo-8L, 13 TOPS) ───────────────────────────────────────
# Install HailoRT SDK from the Hailo Developer Zone:
#   https://hailo.ai/developer-zone/
# Then install Python bindings:
#   pip install hailort  (or use the wheel from the SDK package)

# ── Dev / testing ─────────────────────────────────────────────────────────────
pytest>=7.0.0
pytest-asyncio>=0.23.0
black>=24.0.0
ruff>=0.3.0
